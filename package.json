{
  "name": "easyeda-partdb-extension",
  "version": "0.1.0",
  "description": "EasyEDA Pro extension for Part-DB integration - search parts, check stock, view supplier info",
  "private": true,
  "scripts": {
    "compile": "tsx config/esbuild.prod.ts",
    "build": "npm run compile && tsx build/packaged.ts",
    "watch": "tsx config/esbuild.watch.ts"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "jszip": "^3.10.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "pro-api-types": "latest"
  }
}
